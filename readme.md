# Simple DB Web App

This project can be used as simple learning project. Including:

- EF Core
- MySQL / Maria DB link
- Simple CRUD
- libman

# This version contains
- The base skeleton
- EF Core

# What changed
- Added reference to EF Core
- Changed appsettings.json to add DB Link
- Edit Program.cs to load the DB
- Create DB Context class
- Create a first DB Class
- Create migration via command line

# Requirements
- Have MySQL / MariaDB installed on the PC
- (optional) DataGrid installed for debug