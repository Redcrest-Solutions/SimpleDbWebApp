# Simple DB Web App

This project can be used as simple learning project. Including:

- EF Core
- MySQL / Maria DB link
- Simple CRUD
- libman

# This version contains
- The base skeleton
- EF Core
- Simple CRUD operations

# What changed
- Added CRUD operations on the User table

# Requirements
- Have MySQL / MariaDB installed on the PC
- (optional) DataGrid installed for debug